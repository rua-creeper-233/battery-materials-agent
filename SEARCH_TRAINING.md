# 检索代理的简单“训练”指南

## 先说清楚：当前训练的是什么

当前网页不是自由生成式大模型，而是“可解释检索＋规则工作流”。所谓简单训练，是给一组真实问题标注应命中的论文，再自动选择标题、方法、标签、体系和摘要等字段的检索权重。它能改善排序，而且每个结果都能解释“为什么命中”；它不会让系统凭空学会论文里没有的知识。

## 最小训练流程

1. 打开 `data/search_training.json`。
2. 每条加入一个你真的会问的问题，并在 `relevant` 中写入应命中的论文 ID。
3. 运行 `python train_search.py`。
4. 查看生成的 `data/search_config.json`：重点看 `top1_accuracy`、`mrr_at_5` 和 `recall_at_5`。
5. 用没有参加训练的新问题人工检查。当前文件没有独立测试集，所以指标只能用于回归，不能当成模型泛化能力证明。

建议先积累 30—50 个真实问题，再按约 8:2 分成训练集和保留测试集。问题应覆盖：VASP 入门、正极电压、离子扩散、界面/SEI、热力学稳定性、缺陷、机器学习势、高通量筛选以及“查不到”的负例。

## 立即可用的查询写法

高质量查询通常是：

`材料/体系 + 科学任务 + 计算方法 + 目标输出 + 可选约束`

例如：

- `LGPS + 锂扩散 + AIMD + 扩散系数`
- `LiFePO4 + DFT+U + 平均嵌锂电压`
- `固态电解质界面 + 反应能 + VASP`
- `MACE + 微调 + 不确定性 + 固态电解质`
- `NEB + 迁移势垒 + 超胞收敛`

可组合的显式筛选：

- `tag:DFT`、`tag:MD`、`tag:VASP`、`tag:MS`
- `type:方法论文`、`type:进展论文`
- 示例：`tag:VASP type:方法论文 入门 后处理`

这里的 `MS` 专指 BIOVIA Materials Studio。只有精选元数据明确把它作为方法使用时才标注；背景文字提到软件不算。当前精选库没有满足这一严格条件的论文，所以 `MS` 数量为 0。

## 什么时候接大模型

如果希望回答更像真正的对话助手，下一步应接入“检索增强生成（RAG）”：先由本系统检索论文和页码，再把有限证据交给大模型组织语言，并强制回答逐条带引用。开始时不建议微调大模型；先积累真实问题、人工认可的答案和失败案例，通常比直接训练更划算。API 密钥只应保存在本机环境变量或私有后端，不能写入 GitHub Pages。
