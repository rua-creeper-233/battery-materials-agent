# 检索代理的简单“训练”指南

## 先说清楚：当前训练的是什么

当前网页不是自由生成式大模型，而是“可解释检索＋规则工作流”。所谓简单训练，是给一组真实问题标注应命中的论文，再自动选择标题、方法、标签、体系和摘要等字段的检索权重。它能改善排序，而且每个结果都能解释“为什么命中”；它不会让系统凭空学会论文里没有的知识。

## 最小训练流程

1. 打开 `data/search_training.json`。
2. 每条加入一个你真的会问的问题，并在 `relevant` 中写入应命中的论文 ID。
3. 运行 `python train_search.py`。
4. 查看生成的 `data/search_config.json`：重点看 `top1_accuracy`、`mrr_at_5` 和 `recall_at_5`。
5. 用 `held_out_examples` 中未参与权重选择的问题检查排序，并人工查看失败案例。

当前共有 40 条训练问题、12 条留出问题。权重搜索只读训练集；留出集不参与权重选择。当前留出结果为 MRR@5 0.9167、Recall@5 0.9583、Top-1 0.8333。需要注意：留出集已经用于一次人工词表错误分析，所以只能当回归集，不能宣传成未经接触的泛化测试。继续积累真实问题时，应再冻结一份完全不看的盲测集。

问题覆盖 VASP 入门、正极电压、离子扩散、界面/SEI、热力学稳定性、缺陷、机器学习势、高通量筛选、Materials Studio、ReaxFF、多尺度相场与“查不到”的负例。修改同义词表时，Python 后端与 `static/browser_agent.js` 必须同步，否则本地版和 GitHub Pages 排序会不一致。

## 立即可用的查询写法

高质量查询通常是：

`材料/体系 + 科学任务 + 计算方法 + 目标输出 + 可选约束`

例如：

- `LGPS + 锂扩散 + AIMD + 扩散系数`
- `LiFePO4 + DFT+U + 平均嵌锂电压`
- `固态电解质界面 + 反应能 + VASP`
- `MACE + 微调 + 不确定性 + 固态电解质`
- `NEB + 迁移势垒 + 超胞收敛`

可组合的显式筛选：

- `tag:DFT`、`tag:MD`、`tag:VASP`、`tag:MS`
- `type:方法论文`、`type:进展论文`
- 示例：`tag:VASP type:方法论文 入门 后处理`

这里的 `MS` 专指 BIOVIA Materials Studio。只有精选元数据明确把它作为方法使用时才标注；背景文字提到软件不算。当前严格标注 6 篇，可用 `tag:MS 电解液`、`tag:MS 聚合物电解质` 或 `tag:MS type:进展论文 SEI` 检索。

## 什么时候接大模型

系统已经提供可选的“检索增强生成（RAG）”：先检索论文和页码，再把有限证据交给兼容模型组织语言，并强制回答使用 `[P1]` 格式引用。引用缺失或编号越界时自动回退；当前尚未做逐句事实蕴含审查。启动方式和数据外发边界见 [RAG_SETUP.md](RAG_SETUP.md)。开始时不建议微调大模型；先积累真实问题、人工认可的答案和失败案例，通常比直接训练更划算。API 密钥只能放在本机环境变量或私有后端，不能写入 GitHub Pages。
# 2026-09-16更新

现有43个训练查询、12个历史回归查询；另有14个共享问答回归用例。详见[本轮评估与边界](LEARNING_UPDATE_20260916.md)。这属于检索权重调优与教学规则扩充，不是大模型训练。标注由助手整理，有待研究者复核。以下历史记录中的旧数量以本段为准。
